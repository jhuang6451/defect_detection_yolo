**【角色设定】**
你现在是一位资深的计算机视觉算法工程师和 PyTorch 深度学习专家，精通 YOLO 系列目标检测算法及其底层源码（如基于 Ultralytics 框架），并且拥有丰富的工业级小目标缺陷检测实战及顶级期刊论文辅助代码编写经验。

**【任务背景】**
我正在进行一项名为《基于改进 YOLO 算法的零部件缺陷视觉检测方法研究》的毕业设计。目标是解决工业零部件（如钢带、绝缘子等）质量检测中存在的小目标缺陷特征易丢失、数据集样本不平衡以及噪声标注干扰等问题。

**【执行指令】**
请用 PyTorch 框架为我编写一套完整、模块化、带有标准中文注释的目标检测代码库。整个开发过程请严格按照以下步骤和要求进行：

**第一阶段：构建基准 YOLO 模型与训练管线**
1. **基准搭建**：基于最新的 YOLO26 架构搭建基准（Baseline）模型。
2. **数据管道**：实现数据加载器（数据集位于 `datesets/` ，已做好初步划分与标注），需包含数据增强功能（如 Mosaic 增强、旋转、缩放），以应对样本不平衡问题。
3. **基础管线**：编写 `train.py` 和 `val.py`，实现基础的端到端训练和验证闭环。

**第二阶段：引入核心改进模块（论文创新点）**
1. **引入 SPDConv（空间深度转换卷积）模块**：
   * 在网络的主干（Backbone）中，设计并替换传统的步长卷积（Strided Convolution）和池化层。
   * **要求**：通过代码实现将空间信息整合到通道维度，避免小缺陷特征在下采样和深层网络中丢失。
2. **引入 Wise-IoU-V3（WIoU-v3）损失函数**：
   * 编写 WIoU-v3 损失函数的类。
   * **要求**：实现其动态非单调聚焦机制，根据训练动态调整梯度分配，优先优化高质量样本，抑制低质量样本（噪声标注）的负面影响，并将其嵌入到 YOLO 的损失计算模块中。
3. **特征金字塔（FPN）双向聚合增强（可选/加分）**：
   * 在颈部（Neck）网络中优化 FPN+PAN 结构，实现浅层空间信息与深层语义信息的深度融合。

**第三阶段：代码规范与模块化设计**
1. 结构清晰：请将代码拆分为独立的目录，例如 `models/`（存放网络结构与改进模块）、`utils/`（存放损失函数、度量指标）、`docs/`（文档）、 `eval/` （对比分析）等。
2. 注释标准：所有核心类和函数（特别是 SPDConv 和 WIoU-v3 的实现部分）必须包含 Google 风格的 Docstring 注释，解释张量维度的变化及数学原理。

**第四阶段：完善的参数调优文档（Tutorial/Doc）**
1. 请单列输出一份《超参数调优指南（Hyperparameter Tuning Guide）》。
2. 详细教会我如何针对“小目标缺陷检测”调节以下参数：
   * 学习率（LR）及调度策略的选择。
   * WIoU-v3 特有的超参数（如控制非单调聚焦曲线的 $\alpha$ 和 $\delta$ 参数）应如何根据动态训练情况进行微调。
   * Anchor 尺寸聚类及 IoU 匹配阈值调节。

**第五阶段：输出对比数据与学术图表代码（用于论文写作）**
这是本课题用于毕业答辩和论文撰写的关键部分，请编写独立的 `evaluation.py` 或提供绘图脚本，能够输出以下内容以支持**消融实验（Ablation Study）**：
1. **核心指标计算**：自动统计 Baseline 与改进后模型的 `mAP@0.5`、`mAP@0.5:0.95`、推理速度（`FPS`）、参数量（`Params`）、计算量（`FLOPs`）。
2. **对比图表生成**：绘制并保存用于论文的高清可视化图表：
   * 改进前后的 PR 曲线对比图。
   * 改进前后的 Loss 收敛曲线对比图（突出 WIoU-v3 的加速收敛效果）。
   * 检测效果对比图（同一张验证集图片，左侧基准模型输出，右侧改进模型输出，对比对小缺陷的漏检/误检情况）。

**【输出要求】**
请先向我提供整体的项目目录结构规划，然后依次输出核心模块（`SPDConv` 和 `WIoU-v3`）的 PyTorch 实现代码及相应的参数调优文档。代码请保证可运行性和学术严谨性。请使用中文输出所有文档和注释，使用 `uv` 管理 Python 环境。